{
  "id": "boolean-field",
  "description": "A required boolean value rendered as a checkbox.",
  "schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "open": { "type": "boolean", "title": "Openly accessible" }
    },
    "required": ["open"]
  },
  "uiSchema": {},
  "instances": {
    "valid": [{ "label": "boolean supplied", "data": { "open": false } }],
    "invalid": [{ "label": "string is not boolean", "data": { "open": "false" } }]
  },
  "expectations": {
    "rjsfRendering": { "status": "pass", "reason": "Native boolean field." },
    "formStudioAuthoring": { "status": "pass", "reason": "Native checkbox field." },
    "stapleDeployment": { "status": "pass", "reason": "Used by the research-data built-in." }
  }
}
